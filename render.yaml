services:
  - type: web
    name: xiaguo-ai-agent-mvp
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VISION_PROVIDER
        value: qwen
