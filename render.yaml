services:
  - type: web
    name: xiaguo-ai-agent-mvp
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: VISION_PROVIDER
        value: qwen
