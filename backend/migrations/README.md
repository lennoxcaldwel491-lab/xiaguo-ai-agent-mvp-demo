# Migrations

`001_init.sql` is the baseline schema used by the current backend scaffold.

For local bootstrap you can also run:

```bash
python backend/scripts/init_db.py
python backend/scripts/seed_demo_data.py
```

